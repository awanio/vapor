---
# Kubernetes version (e.g., "1.30", "1.31", etc.)
k8s_version: "1.34"

# Network CIDRs
pod_network_cidr: "10.244.0.0/16"
service_cidr: "10.96.0.0/12"

# CNI plugin: flannel, calico, or cilium
cni_plugin: "flannel"

# Node role: control-plane or worker
node_role: "control-plane"
