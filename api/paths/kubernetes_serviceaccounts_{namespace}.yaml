get:
  summary: List service in a specific namespace
  description: List all service in the specified namespace
  tags:
    - Kubernetes
  security:
    - bearerAuth: []
  parameters:
    - name: namespace
      in: path
      required: true
      description: The namespace to list service from
      schema:
        type: string
  responses:
    '200':
      description: service listed successfully
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
              data:
                type: object
                properties:
                  serviceaccounts:
                    type: array
                    items:
                      $ref: ../components/schemas/ServiceAccountInfo.yaml
                  count:
                    type: integer
                    description: Total number of service in the namespace
                  namespace:
                    type: string
                    description: The namespace that was queried
    '401':
      $ref: ../components/responses/UnauthorizedError.yaml
    '500':
      $ref: ../components/responses/InternalError.yaml
    '503':
      $ref: ../components/responses/KubernetesNotInstalledError.yaml
