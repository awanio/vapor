type: object
required:
  - type
  - cols
  - rows
properties:
  type:
    type: string
    enum:
      - resize
  cols:
    type: integer
    minimum: 1
    description: New terminal width in columns
  rows:
    type: integer
    minimum: 1
    description: New terminal height in rows
