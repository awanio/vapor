allOf:
  - $ref: ./APIResponse.yaml
  - type: object
    properties:
      data:
        type: object
        properties:
          snapshot:
            $ref: ./VMSnapshot.yaml
          message:
            type: string
            example: Snapshot created successfully
