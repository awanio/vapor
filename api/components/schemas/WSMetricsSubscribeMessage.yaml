type: object
required:
  - type
  - metrics
properties:
  type:
    type: string
    enum:
      - subscribe
  metrics:
    type: array
    items:
      type: string
      enum:
        - cpu
        - memory
        - disk
        - network
        - load
    description: List of metrics to subscribe to
