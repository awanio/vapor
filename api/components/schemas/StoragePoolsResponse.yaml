allOf:
  - $ref: ./APIResponse.yaml
  - type: object
    properties:
      data:
        type: object
        properties:
          pools:
            type: array
            items:
              $ref: ./StoragePool.yaml
          count:
            type: integer
