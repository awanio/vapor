allOf:
  - $ref: ./APIResponse.yaml
  - type: object
    properties:
      data:
        type: object
        properties:
          devices:
            type: array
            items:
              $ref: ./PCIDevice.yaml
          count:
            type: integer
            description: Total number of PCI devices
            example: 5
