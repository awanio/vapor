allOf:
  - $ref: ./APIResponse.yaml
  - type: object
    properties:
      data:
        type: object
        properties:
          networks:
            type: array
            items:
              $ref: ./VirtualNetwork.yaml
          count:
            type: integer
