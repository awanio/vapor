type: object
required:
  - action
properties:
  action:
    type: string
    description: The action to perform on the virtual machine
    enum:
      - start
      - stop
      - restart
      - pause
      - resume
      - reset
    example: start
  force:
    type: boolean
    description: Force the action (applicable for stop and restart)
    default: false
    example: false
