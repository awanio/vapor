type: object
required:
  - username
properties:
  username:
    type: string
    description: Username requesting the challenge
  key_fingerprint:
    type: string
    description: Optional SSH key fingerprint to use
