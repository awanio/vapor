type: string
format: yaml
description: |
  Kubernetes Secret object in YAML format with cleaned metadata.
  The following fields are automatically removed for cleaner output:
  - metadata.managedFields (verbose internal field tracking)
  - metadata.selfLink (deprecated field)
  - metadata.uid (auto-generated identifier)
  
  Note: Secret data values are base64 encoded as per Kubernetes specification.
example: |
  apiVersion: v1
  kind: Secret
  metadata:
    name: mysecret
    namespace: default
    resourceVersion: '2345'
    creationTimestamp: '2024-01-15T09:00:00Z'
    labels:
      app: myapp
    annotations:
      kubernetes.io/service-account.name: default
  type: Opaque
  data:
    username: YWRtaW4=
    password: MWYyZDFlMmU2N2Rm
  stringData:
    config.yaml: |
      apiUrl: "https://my.api.com/api/v1"
      timeout: 30
